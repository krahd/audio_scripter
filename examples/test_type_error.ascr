z = x + y; // Should report type error at runtime
y = 1.5;
z = x + y; // Should report type error at runtime;
// Test: Type inference and error reporting
x = true;
y = 1.5;
z = x + y; // Should report type error at runtime;
