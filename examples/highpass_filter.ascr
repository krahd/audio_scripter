# Advanced example using reusable intermediate stages
amount = 0.5 + 0.5 * sin(t * 0.2);
fL = lpf1(inL, 0.08, 0);
fR = lpf1(inR, 0.08, 1);
outL = clip(mix(inL, fL, amount), -0.8, 0.8);
outR = clip(mix(inR, fR, amount), -0.8, 0.8);
