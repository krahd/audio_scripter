// Test: User-defined function, recursion, and type inference
fn factorial(n) {
    if (n <= 1) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
};

result = factorial(5); // Should be 120;
